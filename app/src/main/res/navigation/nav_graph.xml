<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MainMenu">

    <fragment
        android:id="@+id/MainMenu"
        android:name="com.example.wowcharacterselector.MainMenu"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_main_to_classSelector"
            app:destination="@id/classSelector" />
    </fragment>

    <fragment
        android:id="@+id/classSelector"
        android:name="com.example.wowcharacterselector.ClassSelector"
        android:label="ClassSelector" />

</navigation>